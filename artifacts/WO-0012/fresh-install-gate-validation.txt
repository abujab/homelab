WO-0012 fresh-install and rebuild path validation

Target: pi4mB04 only
Date: 2026-07-29

Safety boundary:
- a ten-minute automatic rollback preserved the existing unit, root-only
  environment, enablement link and drop-ins
- /var/lib/rancher/k3s and all persistent worker state remained in place
- the existing K3s binary was reused with INSTALL_K3S_SKIP_DOWNLOAD=true
- K3s binary version remained v1.36.2+k3s1
- K3s binary content remained unchanged before and after the test
- worker join environment content was compared locally under root and unchanged=true
- no token content or token-derived checksum was retained

Validated sequence:
1. k3s-agent.service and the recovery timer were stopped.
2. The installer-owned unit, root-only environment, enablement link and
   Ansible-owned drop-ins were moved to the rollback area.
3. systemd reported the staged agent inactive and disabled.
4. The normal K3s role ran with INSTALL_K3S_SKIP_ENABLE=true and
   INSTALL_K3S_SKIP_START=true.
5. The installer task changed and created the agent unit.
6. Immediate post-install checks reported the agent was not active and not
   enabled.
7. The explicit pre-gate inactive-and-disabled assertion passed.
8. Ansible created the drop-in directory and managed Chrony gate.
9. The handler reloaded systemd and the agent started only through the gated
   unit.
10. The controlled start enabled the agent and asserted active/enabled state.
11. The playbook waited for the worker to return to Ready.

Effective state after validation:
- ActiveState=active
- UnitFileState=enabled
- root-only environment mode=600 root:root
- Requires includes chrony-wait.service
- After includes chrony-wait.service and network-online.target
- TimeoutStartUSec=3min
- Restart=always
- RestartUSec=5s
- health_state=healthy

Cleanup:
- rollback backups and script were removed after successful verification
- the automatic rollback timer was canceled
- no runtime WO-0012 test drop-ins remained
